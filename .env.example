# Use MySQL in production. The API uses local SQLite when this is omitted.
SQL_DATABASE_URL=mysql+pymysql://traffic_user:traffic_password@localhost:3306/traffic_pipeline

# Set false to connect to a running MongoDB server.
MONGO_MOCK=false
MONGO_URI=mongodb://localhost:27017
MONGO_DATABASE=traffic_pipeline

